// Create an array with the names of five fruits.
// Use a for loop to print each fruit in the array to the console.
// Define an array with the names of five fruits
const fruits = ['Apple', 'Banana', 'Cherry', 'Date', 'Elderberry'];

// Use a for loop to print each fruit
for (let i = 0; i < fruits.length; i++) {
    console.log(fruits[i]);
}
